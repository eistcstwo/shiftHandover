/* --- Global Reset/Base Styles (Optional but helpful) --- */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
   /* margin: 20px;*/
    background-color: #f0f4f8;
    color: #333;
}

/* --- The Main Alert Widget (The fixed button) --- */
#alert-widget-container {
    /* Position the widget somewhere prominent, like the top right of the screen */
    position: fixed;
        top: 11px;
    right: 344px;
    z-index: 1001; /* Above the backdrop */
}



.tabs-container {
    display: flex; /* Keeps the flex behavior for child elements */
    margin-bottom: 15px;
    border-bottom: 1px solid #ccc;
    flex-direction: unset;
    /* Centering styles below */
    width: 40%; /* Example width, adjust as needed */
    margin-left: auto;
    margin-right: auto;
}


.tab-btn {
    padding: 32px 186px;
    cursor: pointer;
    border: none;
    background-color: #f1f1f1;
    margin-right: 5px;
    border-radius: 5px 5px 0 0;
    transition: background-color 0.3s;
    font-size: large;

}

.tab-btn:hover {
    background-color: #e0e0e0;
}

.tab-btn.active {
    background-color: #007bff; /* Active tab color */
    color: white;
    border-bottom: 1px solid #007bff;
}


#alert-box {
    display: flex;
    align-items: center;
    padding: 12px 20px;
    background-color: #ff0000;
    color: white;
    border-radius: 8px;
    cursor: pointer;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
    transition: background-color 0.3s ease,
    transform 0.2s ease;
    gap: 10px;
}

#alert-box:hover {
    background-color: #0056b3; /* Darker shade on hover */
    transform: translateY(-2px); /* Slight lift effect */
}

#alert-box .icon {
    font-size: 1.4em;
    /* Use a bell emoji or a proper icon font like FontAwesome */
}

#alert-box .count {
    font-size: 1.1em;
    font-weight: bold;

    padding: 2px 8px;
    border-radius: 50%;
}

#alert-box .message {
    font-weight: 500;
}

/* --- Blink effect for active alerts (only applied if count > 0) --- */
.blink {
    /* Subtle glow/blink rather than color change */
    animation: pulse 1.5s infinite alternate;
}

@keyframes pulse {
  from {
    box-shadow: 0 0 0 0px rgba(0, 123, 255, 0.7);
  }
  to {
    box-shadow: 0 0 0 10px rgba(0, 123, 255, 0);
  }
}

/* --- The Alert Table Overlay (Modal) --- */

#alert-table {
    position: fixed;
    top: 53%;
    left: 50%;
    transform: translate(-50%, -50%);
    background-color: white;
    padding: 30px;
    border-radius: 10px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    z-index: 1002;
    max-height: 150vh;
    overflow-y: auto;
    width: 100%;
    max-width: 2500px;
    display: none;
    box-sizing: border-box;
}

.ip-group-cell {
    vertical-align: top; /* Align content to the top of the merged cell */
    text-align: left;
    background-color: #3b3939; /* Subtle background difference */
    border-right: 1px solid #ccc;
    padding: 10px;
}

/* Ensure checkboxes are sized correctly (as requested previously) */
.resolve-checkbox,
.group-master-checkbox {
  width: 20px;
  height: 20px;
  cursor: pointer;
  vertical-align: middle;
  margin-right: 5px; /* Add space next to the "Select All" text */
}


/* Increase the size of both individual and group checkboxes */
.resolve-checkbox,
.group-master-checkbox {
  width: 20px;
  height: 20px;
  cursor: pointer;
  vertical-align: middle;
}


#alert-table h3 {
    margin-top: 0;
    color: #dc3545; /* Red color for alert title */
    border-bottom: 2px solid #eee;
    padding-bottom: 10px;
}

.table-container {
    max-height: 80vh; /* Responsive height for scrollable area */
    overflow-y: auto;
    margin-bottom: 20px;
}

#alert-table table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 15px;
}

/* Increase the size of the checkbox input itself */
.resolve-checkbox {
  /* Standard size adjustments */
  width: 20px;
  height: 20px;
  /* Makes the checkbox easier to click/interact with on hover */
  cursor: pointer;
  /* Optional: Adjust vertical alignment within the table cell */
  vertical-align: middle;
}


#alert-table th, #alert-table td {
    font-size: 20px; /* Adjust the value as needed */
    padding: 8px; /* Optional: Add some padding for better readability with larger text */
}

#alert-table th, #alert-table td {
    border: 1px solid #ddd;
    padding: 12px;
    text-align: left;
    /* Aligns content nicely within cells */
    vertical-align: middle;
}

#alert-table th {
    background-color: #333131;
    font-weight: 600;
    position: sticky; /* Keeps headers visible when scrolling table */
    top: 0;
    z-index: 99;
}

/* Specific column alignment for better readability */
#alert-table td:nth-child(1) { width: 15%; } /* Server IP */
#alert-table td:nth-child(2) { width: 15%; } /* Broker Name */
#alert-table td:nth-child(3) { width: 15%; } /* EG Name */
#alert-table td:nth-child(4) { width: 55%; } /* Error Message */


/* Close button styling */
#close-table-btn {
    padding: 10px 20px;
    background-color: #6c757d; /* Gray button */
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 1em;
    float: right; /* Aligns button to the right */
}

#close-table-btn:hover {
    background-color: #5a6268;
}


/* --- Backdrop Overlay --- */
/* This needs to be added via JS when the table opens for a proper modal effect */
.backdrop {

    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.6); /* Dark semi-transparent background */
    z-index: 1000;
}
